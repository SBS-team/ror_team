%h1
  LiveChat
%div
  = label_tag "Manager: #{@live_chat.admin_user.email}"
  #chat-history
    - @live_chat.chat_messages.each do |msg|
      .msg
        = msg.body
  .chat-msg
    = form_tag(live_chats_chat_path, method: "post") do
      = label_tag  'Your message*:'
      = text_area_tag :message
      = hidden_field_tag :live_chat_id, @live_chat.id
      = submit_tag 'Send Message'
%div
  = label_tag "params:  #{@p.to_s}"

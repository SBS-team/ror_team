$('#live_chat').replaceWith("<%= escape_javascript(render('shared/live_chat')) %>");

$("#live_chat").draggable({
    handle: "#chat_handle",
    containment: "parent"
});

$('#contact_live_chat').replaceWith("<%= escape_javascript(render('contact/chat')) %>");
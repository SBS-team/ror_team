!!!
%html
  %head
    %title RorTeam Admin Chat
    = stylesheet_link_tag    'application', media: 'all'
    = stylesheet_link_tag    'live_chats'
    = javascript_include_tag 'application'
    = javascript_include_tag 'admin_chat'
    = include_gon
    = csrf_meta_tags
  %body
    - if @live_chat.nil?
      No active chat
    - else
      .row
        .col-lg-10.col-offset-1.form-group
          %h1
            LiveChat with users
          %div
            Client nickname:
            %strong
              = @live_chat.guest_name
          #chat-history.panel
            #chat
              - unless @messages.nil?
                - @messages.each do |msg|
                  - if msg.is_admin
                    .msg.msg-admin
                      = "(#{msg.created_at.strftime('%d-%m-%Y')}) |"
                      %b
                        %U
                          = @live_chat.admin_user.first_name + " " +@live_chat.admin_user.last_name
                      = ": #{msg.body}"
                  - else
                    .msg.msg-user
                      = "(#{msg.created_at.strftime('%d-%m-%Y')}) |"
                      %b
                        %U
                          = @live_chat.guest_name
                      = ": #{msg.body}"
          .chat-msg
            = form_tag(admin_chat_send_msg_path(live_chat_id: @live_chat.id), method: "post", remote: true) do
              Your message*:
              = text_area_tag :message, nil, class: 'form-control', rows: 3
              %br
              = submit_tag 'Send Message', :class => 'btn btn-primary pull-right'
          %div
            = link_to "close chat", admin_chat_close_path(admin_email: @live_chat.admin_user.email), method: :post

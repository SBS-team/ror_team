!!!
%html
  %head
    = favicon_link_tag
    %title RorTeam
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = include_gon(:namespace => 'RorTeam', :camel_case => true)
    = csrf_meta_tags
  %body
    -if !notice.blank?
      .success.message
        %h3
          = notice
    -if !alert.blank?
      .info.message
        %h3
          = alert
    .container
      %header
        .row
          .span2
            .logo
              = link_to(t('.ror_team'), root_path)
          .span6
            - if current_user
              = "Welcome, #{current_user.nickname} "
              = link_to 'Logout', auth_destroy_path
          %nav
            %ul.inline
              %li
                = link_to(t('.company'), company_index_path)
              %li
                = link_to(t('.work'), projects_path)
              %li
                = link_to(t('.team'), team_index_path)
              %li
                = link_to(t('.careers'), jobs_path)
              %li
                = link_to(t('.contact'), contact_index_path)
              %li
                = link_to(t('.blog'), posts_path)
          .chat.span1
            .btn
              = link_to t('.live_chat'), new_live_chat_path, :onclick=>"window.open(this.href,'RoR-Team Live Chat', 'height=500, width=400');return false;"
      #content
        - if flash[:error]
          %p.error
            = flash.now[:error]
        = yield
      %footer#footer
        .contact-us.row
          .span4
            %p.lead
              = t '.use_to_contact'
        .row
          .span6
            %small
              = t '.media_soc'
          .span6.text-right
            %small
              = t '.mail_scype'
        .row.soc_net
          .span4
            %p.lead
              %i.icon-th-large
              = link_to(t('.follow_twitter'), 'http://twitter.com')
          .span4
            %p.lead
              %i.icon-th
              = link_to(t('.follow_facebook'), 'http://facebook.com')
          .span4
            %p.lead
              %i.icon-envelope
              = link_to(t('.contact_email'), '#')
              %br
                = link_to(t('.email'), 'mailto:ror_team@gmail.com')
        .row.text-center
          © 2012-2013 RoR-Team
        .row.bottom
          .span12

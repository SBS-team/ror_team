%p
  Sorry, but now absent any free manager. You can send request on our email
%div
  = form_for @message ||= Message.new, url: contact_index_path(small_window: '1'), html: {class: 'web-form', multipart: true} do |form|
    - if @message.errors.any?
      %div
        %h6
          = pluralize(@message.errors.count, 'error')
          prohibited this message from being sent:
        %ul
          - @message.errors.full_messages.each do |msg|
            %li= msg
    %div
      = form.text_field(:name, placeholder: 'Your name*', class: 'form-control')
    %div
      = form.email_field(:email, placeholder: 'Your email*', class: 'form-control')
    %div
      = form.text_area(:text, placeholder: 'Your message', class: 'form-control' , rows: '5')
    #recaptcha-home
      #recaptcha-div
        = recaptcha_tags display: {theme: 'white'}, ajax: true
    %div
      = submit_tag 'Send e-mail', class: 'btn btn-primary send-email pull-right', data: {disable_with: 'Please wait...'}